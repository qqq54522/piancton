# 服务器部署与验收记录

本文件用于记录每个环境的实际部署结果。操作步骤和判定标准以根目录
`README.md` 的“服务器上线验收清单”为准。

> 当前业务模型和接口状态以 `docs/IMAGE_SEARCH_REBUILD_MASTER_PLAN.md` 为准。本清单未填写前，不得宣称生产部署已经验证。

## 环境信息

- 环境名称：
- 服务器系统：
- 部署日期：
- 操作人：
- 应用域名：
- CORS_ORIGINS：
- Git revision：
- Alembic revision：

## 服务状态

- [ ] Docker Engine 与 Compose 已安装
- [ ] `.env` 已替换默认 PostgreSQL 密码
- [ ] `CORS_ORIGINS` 已设置为最终 HTTPS 域名
- [ ] `SESSION_COOKIE_SECURE=true`
- [ ] `postgres` 健康
- [ ] `backend` 健康
- [ ] `web` 健康
- [ ] HTTPS 证书有效
- [ ] 外层 HTTPS 代理正确转发到 Compose 的 `WEB_PORT`
- [ ] 持久化卷存在

## 功能验收

- [ ] 管理员登录
- [ ] business 权限
- [ ] designer 权限
- [ ] admin 用户管理
- [ ] 图片上传安全校验
- [ ] 图片预览
- [ ] 图片下载与计数
- [ ] 缩略图生成和列表加载
- [ ] 回收站恢复与永久删除
- [ ] 首次上传自动建立素材组，且业务概念和延展尺寸均可后补
- [ ] 素材详情可确认概念关系、追加延展版本并替换主图
- [ ] 业务端只有一个搜索框，六大体系筛选为可选项
- [ ] 同一素材组的多个尺寸不重复铺满结果页
- [ ] 单结果“不相关”反馈能追溯图片和素材组
- [ ] 任一外部搜索增强不可用时数据库路径仍有结果
- [ ] 登录限流与安全响应头
- [ ] 管理员审计日志
- [ ] 容器重启后数据保留

## 备份恢复

- [ ] PostgreSQL 定时备份已配置
- [ ] 图片卷定时备份已配置
- [ ] 数据库、图片卷和部署配置属于同一备份时间点
- [ ] 备份保留周期已确认
- [ ] 恢复演练已完成
- 恢复演练日期：
- 恢复结果：

## 未通过项与处理记录

在此记录服务器环境特有问题，不要把已通过自动测试的代码项重复列为待办。
# TOS 图片存储增量接入（2026-09-07）

使用公司 `ued-zhiku` 桶时，按 [TOS_STORAGE_DEPLOYMENT.md](TOS_STORAGE_DEPLOYMENT.md) 配置、运行真实读写探针再启用后端。默认仍为本地存储；真实桶权限与网络尚未验证，不能据此标记生产验收通过。现有图可按需迁移，不删除数据卷。
